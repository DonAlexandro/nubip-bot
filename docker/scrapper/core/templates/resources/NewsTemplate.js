const ResourceTemplate = require('../ResourceTemplate');

/**
 * Implements standard logic of Resource Template without algorithm modificaiton.
 */
class NewsTemplate extends ResourceTemplate {}

module.exports = new NewsTemplate();
